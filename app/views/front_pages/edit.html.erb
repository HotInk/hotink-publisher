<p class="back_link"><%= link_to '&laquo; Back to front pages list', account_front_pages_path(@account) %></p>

<div id="edit_front_page">
	<% form_for([@account, @front_page]) do |f| %>
	  <%= f.error_messages %>
		<%= render :partial => "form", :locals => { :f => f } %>
	<% end %>
</div>