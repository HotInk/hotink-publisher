<div id="dashboard">
	<h2>Publisher</h2>
	<h1>Control Panel</h1>
	
	<div class="options">
		<%=button_link_to_remote "New front page", :url => new_account_front_page_url(@account), :method => :get, :html => { :class=>"large pill-l" } %><% if @account.current_design -%><%= button_link_to "View site", account_path(@account), :class=>"large pill-c", :target => "_blank" %><%= button_link_to "Edit current design", account_design_path(@account, @account.current_design), :class=>"large pill-c" %><% end -%><%= button_link_to "Change design", account_designs_url(@account), :class=>"large pill-c" %><%= button_link_to "Take site offline", take_offline_account_url(@account), :method => :post, :confirm => "You are about to take you site completely offline. Readers visiting your site will see a notice telling them that your site is temporarily offline.\n\nAre you sure you want to take your site offline?", :class=>"large cancel pill-r" %>
	</div>
	
	<div class="guide">
		<p>From this page you can control the current content and appearance of your publication's website.</p>
		<p><strong>Be careful when making changes</strong> as the changes you make here will instantly affect how your site appears to readers.</p> 
	</div>
	
	<div id="control_panel">
		<div id="front_page_control" class="clearfix">
			<h2>Current front page</h2>
			<% if @account.current_front_page.blank? %>
				<h3 style="margin-left: 60px;">No current front page</h3>
			<% else %>
				<div class="column_holder" style="width: 63%; float: left; clear: left;">
				  <% unless @account.current_front_page.name.blank? %>
					<div class="third_column left_column">
						<h3>Name</h3>
					</div>
					<div class="two_thirds_column right_column">
						<p><%= @account.current_front_page.name %></p>
					</div>
				  <% end %>
					<div class="third_column left_column">
						<h3>Last updated</h3>
					</div>
					<div class="two_thirds_column right_column">
						<% if @account.current_front_page.updated_at > 7.days.ago %>
						<p style="color: green;"><%= "#{@account.current_front_page.updated_at.to_s(:standard)} (#{time_ago_in_words(@account.current_front_page.updated_at)} ago)" %></p>
						<% else %>
						<p style="color: red;"><%= @account.current_front_page.updated_at.to_s(:standard) %></p>
						<% end %>
					</div>
					<div class="third_column left_column">
						<h3>Template</h3>
					</div>
					<div class="two_thirds_column right_column">
						<p><%= @account.current_front_page.template.name %></p>
					</div>
				</div>
				<div class="column_holder" style="width: 30%; float:left;">
					<%= button_link_to "Edit front page", edit_account_front_page_path(@account, @account.current_front_page), :class=>"large extra_wide publish" %>
				</div>
			<% end %>
			
		<!--	<div id="change_front_page">
			<h3>Change</h3>
				<% if @account.designs.blank? %>
					<p>You'll need at least one design in order to create a front page, please <%= link_to "create your first design", new_account_design_path(@account) %></p>
				<% elsif @account.front_pages.blank? %>
					<p><%= link_to "Create your first front page", new_account_front_page_path(@account) %></p>
				<% else %>
					<%- form_for([@account, @press_run], :html => { :onsubmit => "confirm('Are you sure you want to change your current front page? This change will be effective immediately.')" }) do |r| -%>
						<p>You can change your front page by selecting a new one here. <strong>Change your front page as often as you can</strong>, even if you haven't added new content recently.</p>
						<%= r.collection_select :front_page_id, @account.front_pages, :id, :friendly_date, :prompt => "Choose new front page" %>
						<%= button_link_to_submit "Update", :confirm => "Are you sure you want to update your front page? This change will be effective immediately." %>
					<%- end -%>
				<% end %>
			</div> -->
			
		</div>
		<% if @account.current_design %>
			<div id="widget_control" class="clearfix">
				<h2>Current widgets</h2> <%= button_link_to_remote "New", :url => new_account_design_widget_path(@account, @account.current_design), :method => :get, :class=>"micro primary" %>
				<ul id="widgets" class="clearfix">
					<% for widget in @account.current_design.widgets %>
						<li>
							<h4><%= widget.name %></h4>
							<p><%= widget.template.name %></p>
							<h5>Last updated</h5>
							<% if widget.updated_at > 7.days.ago %>
							<p class="updated_at" style="color:green;"><%= time_ago_in_words(widget.updated_at) %> ago</p>
							<% else %>
							<p class="updated_at" style="color:red;"><%= time_ago_in_words(widget.updated_at) %> ago</p>
							<% end %>
							<h5>Templates</h5>
							<% unless widget.templates.blank? %>
							 <ul class="clearfix">
								<% for template in widget.templates %>
								<li><%= link_to template.name, edit_account_design_template_path(@account, @account.current_design, template) %></li>
								<% end %>
							 </ul>
							<% else %>
							  <p><em>Not currently in use.</em></p>
							<% end %>
							<%= button_link_to "Edit widget", edit_account_design_widget_path(@account, @account.current_design, widget), :class=>"large extra_wide" %>
						</li>
					<% end %>
				</ul>
			</div>
		<% end %>
	</div>
	
</div><!-- /#dashboard -->

<% content_for :javascript do -%>
	<script type="text/javascript" charset="utf-8">
		var page_overlay = new Overlay();
		load_button_fix(); //fix firefox button display
	</script>
<% end -%>