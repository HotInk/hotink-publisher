<h1>Listing pages</h1>

<table>
  <tr>
  </tr>

<% @pages.each do |page| %>
  <tr>
	<td>
		<%= link_to page.name, account_page_path(@account, page) %>
	</td>
	<td>
		<%= link_to 'Destroy', account_page_path(@account, page), :confirm => 'Are you sure?', :method => :delete %>
	</td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New page', new_account_page_path(@account) %>
