<% form_for([:account, :article, @comment]) do |f| %>
   <%= f.label :body, 'Body' %>:
   <%= f.text_area :body %><br />
   <%= f.label :name, 'Author' %>:
   <%= f.text_field :name %><br />
   <%= submit_tag 'Create' %>

 <% end %>


<%= fb_login_button('')%>